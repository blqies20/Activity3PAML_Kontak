import 'package:flutter/material.dart';

class KontakHeader extends StatelessWidget {
  const KontakHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar(
      title: const Center(
        child: Text("Registration"),
      ),
    );
  }
}
